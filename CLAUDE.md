@AGENTS.md

> Claude Code and all AI agents must follow `AGENTS.md` for all project rules.
> Key locked facts: canonical domain `https://kaisrun.xyz` (GitHub Pages static export — no SSR, no API routes in production), SAB (no physical storefront shown publicly), one-dog-at-a-time private mobile conditioning service, owner Travis, origin dog Kai (Rhodesian Ridgeback mix).
> Never use `next/image` — static export requires plain `<img>` tags.
> Never reference Vercel — hosting is GitHub Pages only.
> Never use the retired brand name "Emerald Paws Athletic Club" anywhere.
> Run `npm run build` and confirm exit code 0 before reporting task complete.
